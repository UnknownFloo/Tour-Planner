# Copy to .env and adapt values before running the backend.
TOURPLANNER_CONNECTION=Host=localhost;Port=5432;Database=tourplanner;Username=postgres;Password=postgres
JWT_KEY=ReplaceThisWithASecretKeyForJwtSigning
JWT_EXPIRES_IN=60
